printf "held-karp      : "; out/held-karp < $1;
printf "greedy         : "; out/greedy < $1;
printf "best-neighbor  : "; out/best-neighbor < $1;
printf "two-interchange: "; out/two-interchange < $1;
printf "matching       : "; out/matching < $1;